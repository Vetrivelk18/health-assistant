# Web Framework
fastapi==0.115.0
uvicorn==0.24.0
python-multipart==0.0.6

# Google APIs & OAuth
google-auth==2.25.2
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.107.0

# Gemini API (google-genai, not the deprecated google-generativeai)
google-genai==1.47.0

# Telegram Bot — no SDK; services/telegram_bot.py talks to the Bot API
# directly over httpx, so no python-telegram-bot dependency is needed.

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.1

# Task Scheduling: no in-process scheduler. Cloud Run containers don't stay
# alive between requests, so one would silently never fire — Cloud Scheduler
# calls POST /internal/run-daily instead, which fans the batch out over
# Cloud Tasks (one task per user) rather than processing it inline.
# First 1M Cloud Tasks operations/month are free. See DEPLOY.md.
google-cloud-tasks==2.16.3

# Token Management & Security
pyjwt==2.8.0
python-jose==3.3.0
passlib==1.7.4
cryptography==41.0.7

# Environment Variables
python-dotenv==1.0.0

# HTTP Requests
httpx==0.28.1
requests==2.31.0

# Logging: no dependency. Cloud Logging wants a specific JSON shape
# (`severity`, `message`, everything else into jsonPayload) that
# python-json-logger doesn't emit without subclassing its formatter anyway —
# at which point stdlib `logging` + `json` is the smaller thing to maintain.
# See utils/logging_config.py.

# Type Hints & Validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Development (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.12.0
flake8==6.1.0
